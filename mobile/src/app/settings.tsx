import { PlaceholderScreen } from '@/features/shell/placeholder-screen';

export default function SettingsRoute() {
  return (
    <PlaceholderScreen
      title="Settings"
      icon="settings"
      inTabs={false}
      note="Program, block start, targets and backups arrive in a later milestone."
    />
  );
}
