"""Pure fitness domain logic.

INVARIANT: this package must not import ``fitness_lab.storage``,
``fitness_lab.api``, or perform any I/O (filesystem, network, database).
It is deliberately empty in M0 - the real domain model arrives in a later
milestone. ``tests/test_architecture.py`` enforces the import boundary.
"""
